global:
  environment: dev
  namespace: lbg-ns  # ADD THIS LINE

patientService:
  replicaCount: 2
  tag: latest
  serviceType: ClusterIP
  hpa:
    minReplicas: 1
    maxReplicas: 5
    targetCPU: 50    # ADD THESE TWO LINES
    targetMemory: 70 # ADD THESE TWO LINES

appointmentService:
  replicaCount: 2
  tag: latest
  serviceType: ClusterIP
  hpa:
    minReplicas: 1
    maxReplicas: 5
    targetCPU: 50    # ADD THESE TWO LINES
    targetMemory: 70 # ADD THESE TWO LINES

ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
  hosts:
    - host: lbg-app.dev.local

persistence:
  enabled: false
  size: 1Gi
  storageClass: "default"

image:
  credentials:
    username: "acrlbgdemodev"
    password: "cLbic1o0qQPT+vHBDsGWzFBZ+WAN9x+Q5jnPd9DDgK+ACRCLJ5Lt"
  registry: acrlbgdemodev.azurecr.io  # ADD THIS LINE
  pullPolicy: IfNotPresent           # ADD THIS LINE