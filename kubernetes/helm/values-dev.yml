global:
  environment: dev
  namespace: lbg-ns  # ADDED: Missing namespace

patientService:
  replicaCount: 2
  tag: latest
  serviceType: ClusterIP
  hpa:
    minReplicas: 1
    maxReplicas: 5
    targetCPU: 50    # ADDED: Missing HPA targets
    targetMemory: 70 # ADDED: Missing HPA targets

appointmentService:
  replicaCount: 2
  tag: latest
  serviceType: ClusterIP
  hpa:
    minReplicas: 1
    maxReplicas: 5
    targetCPU: 50    # ADDED: Missing HPA targets
    targetMemory: 70 # ADDED: Missing HPA targets

ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
  hosts:
    - host: lbg-app.dev.local

persistence:
  enabled: false
  size: 1Gi
  storageClass: "default"

image:
  credentials:
    username: "acrlbgdemodev"
    password: "cLbic1o0qQPT+vHBDsGWzFBZ+WAN9x+Q5jnPd9DDgK+ACRCLJ5Lt"
  registry: acrlbgdemodev.azurecr.io  # ADDED: Missing registry
  pullPolicy: IfNotPresent           # ADDED: Missing pullPolicy