{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lbg-app-ingress
  namespace: {{ .Values.global.namespace }}
  annotations:
    # Remove deprecated kubernetes.io/ingress.class annotation
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}  # Use new spec field instead of annotation
  rules:
  {{- range .Values.ingress.hosts }}
  - host: {{ .host }}
    http:
      paths:
      - path: /patients
        pathType: Prefix
        backend:
          service:
            name: {{ $.Values.patientService.name }}
            port:
              number: {{ $.Values.patientService.port }}
      - path: /appointments
        pathType: Prefix
        backend:
          service:
            name: {{ $.Values.appointmentService.name }}
            port:
              number: {{ $.Values.appointmentService.port }}
      - path: /health
        pathType: Prefix
        backend:
          service:
            name: {{ $.Values.patientService.name }}
            port:
              number: {{ $.Values.patientService.port }}
  {{- end }}
{{- end }}